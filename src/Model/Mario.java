package Model;

public class Mario extends Hero{
    public Mario(int lives, double[] coordinates, int height, int width, double[] velocity, int score,int coin) {
        super(lives, coordinates, height, width, velocity, score, coin);
    }
}
